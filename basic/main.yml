preplay:
  renderer: jinja2
kalpa:
  import:
    config:
      file: .config.yml
      ref:  vars
      directory: ../

  play:
    kalpa-execa:
       - name: Touch a file
         kalpa-execa:
           cmd: touch
           opts:
              - foo.txt
